# Môi trường test — volume tí hon cho unit test chạy nhanh.
paths:
  data_root: dataset
  artifacts_root: artifacts

data:
  max_slices: 8
  volume_size: [32, 32]
  max_series: 2

model:
  student_dim: 64
  student_depth: 2
  student_heads: 4
  student_mlp_dim: 128
  teacher_dim: 32
  guidance_dim: 48
  contrast_dim: 16
  patch_size: [4, 8, 8]

train:
  batch_size: 2
  epochs: 1
  num_workers: 0
  amp_dtype: none
