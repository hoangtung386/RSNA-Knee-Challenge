# Môi trường local — dataset mô phỏng nhỏ để smoke test.
# Đường dẫn tương đối được resolve theo thư mục gốc của project.
paths:
  data_root: dataset
  artifacts_root: artifacts
  dino_code_dir: vendor/3DINO

train:
  batch_size: 2
  num_workers: 0
  amp_dtype: none    # local thường không có GPU

data:
  max_slices: 32
  volume_size: [64, 64]
